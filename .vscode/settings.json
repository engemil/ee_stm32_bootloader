{
    // Bootloader Settings
    "settings.bootloader.folder": "${workspaceFolder}/bootloader", // Path to bootloader project folder

    // OpenOCD Settings
    "settings.openocd.interface": "interface/stlink.cfg", // OpenOCD Debugger interface file
    "settings.openocd.board": "board/st_nucleo_c0.cfg", // OpenOCD Board file

    // Flash Address Settings
    "settings.flash.bootloaderAddress": "0x08000000" // Bootloader flash start address

}
