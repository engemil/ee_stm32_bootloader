# boards

This folder contains custom board files. Board files included in the ChibiOS repository can be found under `ChibiOS/os/hal/board/`.

Remember to update your projects `Makefile`, when changing board files.
